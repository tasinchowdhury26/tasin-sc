import React from 'react';
import styles from '../styles/Footer.module.css'

const Footer = () => {
    return (
        <div>
            <h1 className={styles.name}>Footer</h1>
        </div>
    );
};

export default Footer;